# ============================================================
# ParkinsonTap-GP — Variables de entorno
# cp .env.example .env
# ============================================================

# --- Base de datos PostgreSQL ---
DB_USER=user
DB_PASSWORD=password
DB_NAME=parkinsontap
DB_HOST=localhost
DB_PORT=5433

# --- MQTT Broker ---
MQTT_HOST=localhost
MQTT_PORT=1883

# --- Backend Spring Boot ---
SPRING_PORT=8080
JWT_SECRET=cambia_esto_por_un_secreto_seguro_de_al_menos_32_chars

# --- Frontend React ---
VITE_API_URL=http://localhost:8080/api
VITE_MQTT_HOST=localhost
VITE_MQTT_PORT=9001

